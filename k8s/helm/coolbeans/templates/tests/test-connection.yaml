apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "coolbeans.fullname" . }}-test-connection"
  labels:
    {{- include "coolbeans.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "coolbeans.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
